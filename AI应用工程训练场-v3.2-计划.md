# AI 应用工程训练场 v3.2 计划与交付记录

## 版本定位

v3.2 聚焦“任务真正可执行、个人进度可迁移、资源筛选可分享”。它承接 v3.1 的桌面布局与导航修复，不重新设计品牌视觉，也不引入后端、账号或云同步。

## 本版本目标

| 目标 | 交付内容 |
|---|---|
| 本地状态统一 | 增加 `radar-workbench-v1` 聚合快照，并兼容旧版状态 key |
| 任务执行增强 | 阶段门槛、开始前说明、证据目录命令、步骤提示、验收标准 |
| 进度迁移 | 工作台新增 JSON 导入，校验 v3 备份结构后恢复状态 |
| 报告导出 | 保留 JSON 导出，并增加 Markdown 学习报告 |
| 资源探索 | 增加主语言筛选、Stars/最近更新排序和 URL 状态同步 |
| 路由稳定性 | 非法路线不再静默展示第一条路线，改为明确返回入口 |

## 具体功能

### 统一状态与迁移

任务状态、步骤勾选和收藏继续兼容现有 `radar-status-v3`、`radar-checks-v3`、`radar-favorites-v3`，同时通过 `radar-workbench-v1` 保存带版本号和更新时间的聚合状态。读取时优先使用已有数据，旧用户不需要清空浏览器数据即可继续使用。

JSON 导入只接受 `version: 3` 且 `status` 为对象的备份文件；无效文件会提示失败，不会覆盖当前状态。导入成功后刷新页面，工作台和任务页读取恢复后的状态。

### 任务执行闭环

路线详情新增阶段门槛说明，强调任务完成必须留下代码、运行日志或评测结果，不能把打开外链视为完成。任务面板新增“开始前”信息和证据目录命令，例如 `evidence/api-01/README.md`，并提示用户将步骤执行结果保存到该目录。

任务仍提供进行中、完成、稍后处理三种状态、步骤级勾选、完成验收标准、建议产出物、常见偏差和关联资源。路线详情通过 `?task=` 直达指定任务，非法路线显示明确的路线不存在状态。

### 资源筛选与分享

资源探索新增主语言筛选和排序方式：默认、Stars、最近更新。搜索词、分类、主语言和排序条件写入 URL 的 `q`、`category`、`language`、`sort` 参数；资源详情继续使用 `resource` 参数。由此可以分享一个具体的资源检索上下文，而不是只能分享页面首页。

### 工作台报告

工作台保留 JSON 备份下载，并增加 Markdown 报告下载。Markdown 报告包含导出时间、总进度、已完成数量、进行中数量、收藏数量、推荐下一步和进行中任务清单。报告不自动生成虚构的项目指标或学习成果。

## 验证计划与执行结果

本版本运行 `pnpm check` 和 `pnpm build`，均通过。生产构建仍有 Vite 关于 bundle 大于 500KB 的性能提醒，但不影响构建成功；后续可以通过动态导入拆分页面。

桌面端已验证首页宽度、路线详情任务直达、任务面板开始前信息、证据入口、导航高亮和新增筛选控件。移动端使用 390×844 视口生成回归截图，确认单列页面仍可加载。工作台 JSON/Markdown 导出控件和 JSON 导入处理已通过类型检查。

## 已知限制

第一，统一状态目前采用聚合快照加旧 key 兼容写入的渐进迁移方式，下一版本可以进一步改成单一写入源。第二，任务证据目录命令只是通用起点，不会替用户运行本地模型、向量数据库或评测工具。第三，资源排序使用当前 GitHub 快照字段，没有历史快照时不会展示增长曲线。第四，项目仍是纯静态网站，不支持账号、云端同步、协作或跨设备实时同步。

## v3.3 建议

下一版本可以继续增加任务前置条件、依赖关系和下一任务推荐；为核心任务补充 Python/TypeScript 可复制示例；工作台增加本周计划、阻塞记录和证据清单；资源探索增加岗位能力、难度和预计投入时间筛选；再完善抽屉焦点管理与完整键盘操作。
