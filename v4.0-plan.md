# AI 应用工程训练场 v4.0 计划

## 版本定位

v4.0 将项目从纯静态个人研究副本升级为可登录的云端工作台，解决 v3.x 的核心限制：状态只能保存在当前浏览器、无法跨设备同步、无法查看历史变化。本版本使用 WebDev 的 `web-db-user` 能力，沿用现有 React 页面和 Signal Library 视觉，不推倒重做。

## 目标

用户可以使用 Manus OAuth 登录，在多个设备上读取和保存自己的学习进度、收藏、作品集记录、本周计划、阻塞记录和任务证据；系统自动保存带时间戳的历史快照，用户可以选择两个快照进行字段级对比。未登录用户仍可以浏览公开研究资源，并继续使用本地状态作为临时体验。

## 技术方案

1. 将当前 WebDev 静态项目升级为 `web-db-user`，使用内置 Manus OAuth、tRPC、Drizzle 和数据库。
2. 新增用户专属工作台数据表，所有读写接口使用 `protectedProcedure` 并按 `ctx.user` 隔离。
3. 设计 `workbench_state` 表保存当前聚合状态：任务状态、步骤勾选、收藏、作品集项目、本周计划和阻塞记录，使用 JSON 字段承接 v3.x 数据结构并保存 `updated_at`。
4. 设计 `workbench_snapshots` 表保存每次用户主动保存或关键变更后的完整状态、标签和创建时间；保留最近 30 个快照，提供列表和详情接口。
5. 增加 `syncStatus`、`lastSyncedAt`、冲突提示和“保存快照”动作。首次登录时提供“导入本地数据”选项，不自动覆盖云端数据。
6. v3.x LocalStorage 作为迁移来源，登录后用户可以预览本地数据摘要，再确认上传到云端；未确认前不覆盖云端已有数据。

## 页面与交互

### 登录与同步

顶部增加登录/用户状态入口；登录后显示同步状态、最近同步时间和退出入口。网络失败时保留本地操作并提示稍后重试，不伪装成已同步。公开页面无需登录，工作台、作品集和历史快照在未登录时显示明确的登录引导。

### 我的工作台

工作台增加“云端同步”卡片、最后同步时间、待上传提示和“从本地导入”按钮。同步成功、失败、冲突和退出状态均有可读反馈。原有本地导出功能继续保留，作为备份和迁移兜底。

### 历史快照

新增 `/history` 页面，展示快照时间、来源标签和摘要统计。支持选择基准快照与对比快照，按任务进度、收藏、作品集、本周计划和阻塞记录分类显示新增、删除和修改；JSON 字段差异提供可读文本，不直接暴露内部数据库细节。

### 作品集同步

作品集记录改为云端优先，登录后支持跨设备编辑；每次保存保留快照入口。未登录仍可使用 `radar-portfolio-v1` 本地草稿，并可导出 JSON 后再导入云端。

## 数据安全与边界

只读取当前登录用户自己的数据；公开资源和 GitHub 研究快照不写入用户表。OAuth、会话和数据库连接使用 WebDev 内置能力，不在前端处理 Cookie 或提交密钥。删除项目或清空工作台前显示明确确认；快照默认只读，避免误改历史记录。当前版本不做团队共享、公开作品集权限、实时协作或第三方账号绑定。

## 实施顺序

1. 先保存本计划文档，并将静态 WebDev 项目升级为 `web-db-user`。
2. 阅读生成的 schema、db、router 和 auth 结构，增加工作台状态与快照表及迁移。
3. 实现 tRPC 查询/保存/快照/对比接口和服务端测试。
4. 将前端状态层接入登录状态和云端查询，保留本地 fallback 与迁移预览。
5. 新增同步卡片、登录引导、历史快照列表和差异视图。
6. 更新作品集和工作台，使其支持云端优先、导出兜底。
7. 更新 README，说明登录、数据隔离、迁移、快照保留规则和纯静态限制已变为云端能力边界。
8. 运行 `pnpm check`、`pnpm build`、`pnpm test`，验证未登录浏览、登录、同步、失败提示、迁移、快照对比和移动端。
9. 保存 WebDev 检查点并正式发布；同步 GitHub 源仓库中的前端、schema、router、测试和本计划文档。

## 验收标准

用户可以登录并看到自己的工作台；在一台设备修改任务或作品集后，另一台登录同一账号可以读取；本地数据迁移不会无提示覆盖云端；保存两个快照后可查看任务、收藏、作品集、计划和阻塞的差异；退出登录后私有页面不展示他人数据；网络异常有明确提示并保留本地备份能力；类型检查、构建和服务端测试通过。

## 风险与限制

v4.0 依赖 WebDev 数据库和 OAuth 配置，若当前项目无法原地升级，需要创建同名迁移项目并重新发布。数据库 schema 变更必须先生成并应用迁移。历史快照只代表用户在系统中保存的状态，不是 GitHub 数据历史。云端同步不等于实时协作；本版本不提供团队权限和公开分享。


## 实施记录

已完成 WebDev 项目从静态模板到 `web-db-user` 的能力升级，启用 Manus OAuth、tRPC、Drizzle 和数据库；新增用户专属 `workbench_states` 与 `workbench_snapshots` 表，快照默认最多保留 30 条。前端新增登录引导、云端状态保存/恢复、手动创建快照和 `/history` 字段对比页面，并保留本地导出兜底。

数据库迁移已生成并审阅，使用 `CREATE TABLE IF NOT EXISTS` 应用到当前项目数据库，无 DROP 或数据破坏性操作。类型检查已通过；最终交付前还需运行生产构建和服务端测试，并在未登录工作台、登录入口、同步反馈和历史页面完成浏览器回归。
