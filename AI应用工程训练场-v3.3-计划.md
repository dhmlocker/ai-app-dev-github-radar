# AI 应用工程训练场 v3.3 计划与交付记录

## 版本定位

v3.3 继续围绕“更容易开始、更容易坚持、更能证明能力”推进。它承接 v3.2 的 JSON 导入、资源筛选和任务门槛，将工作台从进度看板推进为轻量行动控制台。

## 本版本交付

| 方向 | 交付内容 |
|---|---|
| 任务执行 | 阶段门槛、前置任务提示、证据保存提示和核心 API 任务示例命令 |
| 工作台 | 最近完成的证据清单、阻塞记录输入与本地保存、继续学习入口 |
| 状态 | 阻塞记录并入版本化工作台快照同步流程，保留旧 key 兼容 |
| 资源 | 继续保留 v3.2 的主语言筛选、Stars/最近更新排序和 URL 分享 |
| 可维护性 | 增加 v3.3 文档并记录纯静态部署边界 |

## 任务依赖与推荐

路线详情增加阶段门槛提示：本阶段不以阅读链接为完成标准，而要留下代码、运行日志或评测结果。任务开始前提示建议先完成同一路线的上一项任务；工作台继续根据第一个未完成任务给出下一步入口。核心模型/API 任务提供 Python 虚拟环境和 `demo.py` 起步命令，其他任务继续提供按任务编号生成证据目录的命令模板。

## 工作台行动层

工作台新增“我的证据清单”，聚合已完成任务的建议产出，并可跳转回对应任务；新增“阻塞记录”，用户可以记录当前问题和下一次尝试，最近记录保存在浏览器 LocalStorage，并同步到工作台聚合快照。已有 JSON 和 Markdown 导出继续保留。

## 验证计划与结果

本版本运行 `pnpm check` 和 `pnpm build`，均通过。桌面端验证路线详情任务面板、阶段门槛、任务示例命令和工作台证据区；移动端沿用 390×844 单列回归目标，重点检查新增工作台区域不造成横向溢出。资源探索继续验证筛选控件和 URL 参数恢复。

构建仍提示 JavaScript bundle 大于 500KB，这是性能优化提醒，不是构建失败；后续可通过页面级动态导入拆分资源。

## 已知限制

当前版本仍采用聚合快照与旧 key 兼容写入的渐进迁移方案，尚未完全删除旧 key。阻塞记录提供本地文本记录和最近条目展示，不支持云同步、协作或提醒。任务示例覆盖核心 API 入口，RAG、Agent、评测和部署任务仍需要后续分别补充更完整的 Python/TypeScript 可运行模板。项目仍为纯静态网站。

## v3.4 建议

下一版本可将状态正式收敛到单一 `training-ground-state` 写入源；增加任务前置条件的结构化字段、阶段完成门槛和自动推荐；为工作台增加本周计划、截止日期和阻塞状态；补充更多任务的复制命令与可下载模板；通过动态导入改善生产包体积。
