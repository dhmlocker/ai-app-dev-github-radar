# AI 应用工程训练场 v3.5 计划

## 版本定位

v3.5 聚焦“把学习证据整理成可展示的作品集”。它承接 v3.4 的资源质量和 v3.3 的工作台闭环，不新增账号或后端服务，先在纯静态网站内完成产出记录、项目总结模板和可复制的静态作品集草稿。

## 目标

用户完成学习任务后，可以记录真实产出，说明项目背景、技术取舍、验证结果和限制，并一键生成适合继续编辑的 Markdown 项目总结。工作台显示作品集项目数量和最近产出，作品集页面可直接查看、编辑、复制和导出。

## 功能范围

### 1. 产出记录

新增独立 `/portfolio` 页面和作品集项目列表。每条记录至少包含项目名称、项目类型、关联路线/任务、项目状态、项目简介、目标用户或场景、使用技术、关键功能、个人负责内容、验证证据、结果指标、已知限制、演示链接、代码仓库链接和更新时间。记录保存在浏览器 LocalStorage，可新增、编辑、删除和复制。

### 2. 项目总结模板

提供结构化编辑表单，默认模板包含：问题背景、目标、方案与架构、关键取舍、实现过程、评测/验证、失败与改进、部署方式、项目边界、下一步。表单旁展示填写提示，避免生成虚构指标；没有真实数据时必须允许填写“未测量”或“待补充”。

### 3. 静态作品集草稿

增加“预览草稿”视图，把记录渲染成简洁的项目案例页。支持复制 Markdown 和下载 Markdown/JSON；导出的内容明确标记为个人草稿，不宣称外部评价或就业结果。默认示例使用当前真实任务产出字段，不伪造用户成果。

### 4. 工作台联动

工作台增加作品集摘要、最近更新项目和“把已完成任务整理成项目”入口。完成任务的证据清单继续作为项目总结的候选素材，点击后跳转到作品集新建表单并预填任务标题、阶段、建议产出和关联仓库。作品集保存后返回工作台可看到记录数量。

### 5. 数据迁移与纯静态限制

新增版本化键 `radar-portfolio-v1`，读取时兼容空数据，不覆盖已有任务状态。JSON 备份和恢复扩展作品集字段；导入失败不覆盖当前数据。README 明确说明记录仅保存在当前浏览器，跨设备需要手动导出 JSON/Markdown，v4.0 才考虑云端同步。

## 视觉方案

沿用 Signal Library：暖白档案纸、深墨、苔绿、信号橙、Noto Serif SC、IBM Plex Sans、DM Mono。作品集页面采用左侧项目索引、右侧编辑/预览的非对称布局；空状态提供明确的第一条记录动作；表单使用分组字段和编号标签，避免普通 SaaS 表单感。

## 实施顺序

1. 审计当前任务、工作台状态和路由结构。
2. 增加作品集类型、LocalStorage 读写和迁移辅助函数。
3. 实现作品集列表、编辑表单、删除/复制、Markdown/JSON 导出。
4. 实现预览草稿和任务证据预填入口。
5. 将作品集摘要和最近项目接入工作台。
6. 更新 README 和本计划文档的交付记录。
7. 运行 `pnpm check`、`pnpm build`，验证桌面与 390px 移动端。
8. 提交 GitHub、保存 WebDev 检查点并正式发布。

## 验收标准

用户可以创建一条项目记录，编辑所有核心字段，刷新页面后记录仍存在；可以从任务进入新建表单并获得预填内容；可以查看预览草稿，复制或下载 Markdown/JSON；可以删除和复制项目；工作台能显示项目数量与最近项目；移动端表单不横向溢出；导入非法 JSON 不会覆盖状态；类型检查和生产构建通过。

## 风险与限制

纯静态模式无法提供账号、云同步、协作或自动验证链接。作品集结果指标完全由用户填写，系统不自动生成或推断；当前版本只提供模板和本地草稿。若未来增加公开分享，应在 v4.0 设计隐私和权限边界后再实施。


## 实施记录

本版本已实现独立作品集页面 `/portfolio`、任务预填、项目编辑表单、项目卡片、删除与 Markdown/JSON 下载，并在工作台增加作品集数量和进入入口。数据使用 `radar-portfolio-v1` 保存，保持纯静态浏览器本地模式。下一步验证包括表单保存、刷新恢复、任务预填、导出下载、移动端不溢出以及工作台入口联动。
