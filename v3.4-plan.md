# AI 应用工程训练场 v3.4-plan

## 目标

v3.4 先解决工作台页面“信息堆叠、卡片同质、操作层级弱、空状态单薄”的 UI 问题，再按版本节奏推进资源与数据质量。工作台继续使用暖白档案纸、深墨、苔绿与信号橙，不改成普通 SaaS 仪表盘。

## 第一部分：工作台 UI 优化

### 信息架构

工作台改为“行动摘要—本周计划—继续学习—证据与阻塞—迁移工具”的顺序。顶部使用一个明确的进度摘要区域，展示总进度、进行中任务、收藏资源和本周计划数量。摘要卡片都提供可点击入口或明确动作，不再只是静态数字。

### 视觉与布局

桌面端采用 12 列编辑式网格：左侧为“今天继续”主行动卡，中间为阶段进度和本周计划，底部并排显示证据清单与阻塞记录。降低大面积深色卡片的比例，使用细边框、编号索引、状态色和更紧凑的行式列表来提升信息密度。移动端保持单列，但保证行动卡、计划列表和输入区域不横向溢出。

### 交互

本周计划支持勾选、最多 3 项并保存；阻塞记录支持添加、删除最近记录和清空；证据条目可跳转至任务详情；继续学习直接进入下一项未完成任务；导入 JSON 前显示格式和覆盖范围提示，导入失败不覆盖已有状态。所有操作使用现有 LocalStorage，不新增后端依赖。

## 第二部分：v3.4 资源与数据质量

### 高级筛选与排序

资源探索增加资源类型、学习阶段、岗位能力、难度、预计投入时间和维护新鲜度筛选；继续支持关键词、分类、主语言、Stars 和最近更新排序。筛选状态全部写入 URL，支持复制链接恢复。

### 精选清单

新增“精选清单”概念，按明确的选择理由区分核心实践仓库、官方文档、课程、Playbook、论文/方法论和作品集模板。页面显示保留理由、适合任务、数据快照日期和访问状态；不把官网索引或单纯 SDK 首页伪装成实践项目。

### 质量报告与选择标准

新增 `/quality` 页面，公开样本数量、字段完整率、许可证覆盖、最近维护分布、待复核链接、重复/同质项目和删除原因。新增 `/selection` 页面，说明仓库保留和移出规则、外部资源验证规则、快照口径、GitHub 活跃度信号边界和不虚构评价的原则。

### 数据字段

为资源记录补充或核验 `selectionReason`、`handsOnEntry`、`officialDocsUrl`、`resourceType`、`keepStatus`、`lastVerifiedAt`、`status`、`sourceAuthority`、`stage`、`skills`、`difficulty` 和 `estimatedHours`。所有新增字段必须有真实来源或明确的人工核验说明。

## 实施顺序

1. 先检查工作台桌面与移动端截图，建立 UI 问题对照表。
2. 重排工作台 JSX 与 CSS，保留既有状态逻辑和品牌视觉。
3. 补齐计划、证据、阻塞、导入导出交互，并验证 LocalStorage 刷新恢复。
4. 整理资源筛选数据结构和 URL 参数。
5. 增加质量报告与选择标准页面，使用现有真实快照，不伪造历史数据。
6. 更新 README，说明 v3.4 数据口径、筛选规则和纯静态限制。
7. 运行 `pnpm check`、`pnpm build`，验证 1280/1440/1920 桌面断点与 390 移动端。
8. 提交 GitHub、保存 WebDev 检查点并发布线上版本。

## 验收标准

工作台首屏能在不滚动的情况下看到下一步行动和核心进度；用户可以添加并清理阻塞记录，勾选本周计划，点击证据跳转任务，导出并导入 JSON；刷新后状态保持。资源探索的高级筛选、排序和 URL 恢复可用；质量报告和选择标准页面可直接访问；类型检查和生产构建通过。

## 风险与限制

项目仍是纯静态网站，不提供账号、云同步、协作和定时抓取。GitHub 数据质量报告只反映当前公开快照，不等价于就业市场统计。外部资源可能发生改版或失效，因此页面必须保留核验日期和待复核状态。由于当前页面组件集中在 `Workbench.tsx`，本次优先做稳定的局部重排，不进行高风险的大规模架构迁移。


## 实施记录

本轮已完成工作台 UI 重排：本周计划提升为深色行动区，证据清单与阻塞记录采用并列信息卡，移动端回落为单列；同时加入质量报告 `/quality` 和选择标准 `/selection` 两个独立入口，并把它们加入顶部导航。质量报告展示当前样本、许可证覆盖、90 天维护信号和归档数量；选择标准公开保留、去重、许可、动手入口和外部资源核验规则。

验证时将重点检查工作台首屏层级、计划勾选、阻塞记录输入、证据跳转、质量报告与选择标准直接访问，以及 390px 移动端不横向溢出。数据仍只使用已有 GitHub 快照字段，不新增虚构历史数据。
