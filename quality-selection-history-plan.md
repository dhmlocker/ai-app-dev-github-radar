# 质量报告、选择标准与历史快照功能化改造计划

## 一、问题判断

当前三个页面已有路由和基础文案，但仍停留在说明页层面：质量报告只有汇总数字，无法定位具体质量问题；选择标准只有原则列表，不能记录用户判断或形成精选清单；历史快照在未登录时只有登录引导，登录后也缺少创建、命名、摘要和可读差异解释。

本次改造不重做现有品牌视觉，重点让三个页面分别承担“发现问题、做出选择、复盘变化”的工作流。

## 二、质量报告 `/quality`

1. 基于真实 GitHub 快照增加指标卡：总样本、描述完整率、许可证明确率、90 天维护率、归档数量、关键字段缺失数、重复/同质候选数。
2. 增加问题筛选：全部、字段缺失、许可证待确认、90 天未推送、已归档、描述过短、缺少 Topics、缺少岗位适配说明。
3. 增加可排序问题清单，显示仓库、分类、问题类型、真实字段值、快照日期和原始 GitHub 链接；点击条目跳转资源探索并携带 `repo` 或筛选参数。
4. 增加按分类的问题分布，点击分类联动问题清单。
5. 增加指标口径展开区，说明 `NOASSERTION`、`pushed_at` 和归档状态的计算方式，明确这不是绝对质量评分或就业统计。
6. 状态使用“通过、需复核、风险提示”，不把数据缺失自动等同于项目不可用。

## 三、选择标准 `/selection`

1. 将六条标准改为可展开卡片，每条包含判断问题、所需证据、通过条件、常见误判和适用范围。
2. 增加选择工作流：用户选择仓库后，逐条标准标记“通过、待核验、不适用”，状态保存到 `radar-selection-v1`。
3. 选择时展示真实摘要：Stars、Forks、开放 Issue、主语言、许可证、最近推送、归档状态、Topics、关联任务和岗位适配说明。
4. 增加精选清单结果区，支持导出 Markdown/JSON；导出必须包含证据、快照日期和未完成核验项。
5. 对存在质量问题的仓库提供跳转质量报告入口；缺失字段显示待复核，不静默判定为不合格。
6. 记录选择的仓库和标准判断，不修改公共研究数据。

## 四、历史快照 `/history`

1. 登录后显示当前状态摘要：完成任务、进行中任务、收藏、作品集、本周计划和阻塞记录。
2. 增加“立即保存快照”动作，可填写名称和备注；保存前显示摘要，成功后刷新列表。
3. 快照列表显示名称、时间、来源、摘要统计，并提供查看详情、设为基准、设为对比和删除。
4. 对比视图按任务、收藏、作品集、本周计划、阻塞记录显示新增、删除和修改数量；展开后显示具体条目变化，而不是直接堆 JSON。
5. 增加“无变化”状态、单快照提示和无快照行动引导。
6. 增加 `?base=...&compare=...` URL 状态；参数只引用当前用户可访问的快照 ID，不把私有数据放进 URL。
7. 删除快照前确认，删除不影响当前工作台状态；继续保留最近 30 条规则。

## 五、后端与共享数据

1. 在现有快照表增加结构化的 `note`、`source` 字段；若迁移不适合扩表，则将其放入状态 envelope，但 API 必须返回结构化字段。
2. 增加 `workbench.summary`、`workbench.snapshotDetail`、`workbench.deleteSnapshot` 和快照对比所需接口，全部使用 `protectedProcedure` 并按用户 ID 隔离。
3. 将质量检查、选择状态和快照 diff 抽成共享纯函数并覆盖单元测试。
4. 保留 v4.0 登录、云端优先和本地导出兜底；未登录用户继续浏览公开页面，私有页面显示可行动登录引导。

## 六、实施顺序

1. 审计当前页面、LocalStorage 键、快照 API 和数据字段。
2. 抽取共享类型和纯函数，先补测试。
3. 实现质量报告的问题筛选、清单、分类联动和资源详情跳转。
4. 实现选择标准的逐条判断、仓库选择、LocalStorage 持久化和导出。
5. 实现快照创建、命名、摘要、结构化对比、删除和 URL 状态。
6. 更新 schema，生成并审阅 SQL，再应用非破坏性迁移。
7. 更新 README 与版本说明。
8. 运行 `pnpm check`、`pnpm build`、`pnpm test`，回归桌面与移动端。
9. 验证登录、用户隔离、空状态、错误状态、筛选联动、导出和快照对比。
10. 提交 GitHub、保存 WebDev 检查点并正式发布。

## 七、验收标准

质量报告可以定位具体仓库问题并跳转资源详情；选择标准可以对仓库逐条记录判断，刷新后保留并导出带证据和快照日期的清单；登录用户可以创建至少两个命名快照并看到可读的新增/删除/修改差异；未登录、无快照和单快照状态都有下一步动作；删除和数据读取均按账号隔离；桌面信息密度合理，移动端不横向溢出；检查、构建和测试通过。

## 假设与限制

本次继续使用 Manus OAuth、现有数据库和 tRPC，不增加第三方账号体系。质量报告反映当前公开快照，不是绝对质量结论；选择标准记录用户判断，不是平台背书；历史快照记录用户工作台，不是 GitHub 历史 Star 数据。公开分享仍不包含私有快照内容。完成后建议将版本标记为 **v4.1：研究质量与个人复盘**，把公开作品集和团队协作留给后续版本。
