# AI 应用工程训练场 v3.1 计划

## 版本定位

v3.1 是一次“桌面体验与学习闭环增强”版本。目标不是新增更多孤立页面，而是让用户在桌面端更容易判断当前所在位置、打开下一项任务、留下执行证据，并把自己的研究状态保存在本地。

## 本版本目标

1. 解决旧版桌面页面继承双列布局后出现的窄列、标题逐字换行和大面积空白问题。
2. 让导航、路线详情、任务详情和资源筛选具备更清晰的上下文。
3. 让任务详情从一句说明升级为可开始执行的任务面板。
4. 让本地进度拥有版本化聚合快照，并继续兼容旧版本数据。
5. 让工作台可以导出 JSON 备份和 Markdown 学习报告。

## 已实现功能

### 桌面端布局

- 覆盖旧版 `.page-frame` 的遗留双列规则。
- 统一页面最大宽度、内容宽度和桌面端横向布局。
- 首页、资源探索、路线总览、路线详情、工具训练、面试准备、能力提升和工作台均恢复正常桌面信息密度。
- 保留移动端单列布局，并增加小屏任务开始信息的响应式处理。

### 导航与页面上下文

- 顶部导航根据当前路由显示 active 状态。
- 路线详情页中 `/paths` 导航会在所有路线子页面保持高亮。
- 路由切换后页面自动回到顶部。
- 移动端菜单点击导航后自动关闭。
- 菜单按钮、抽屉关闭按钮和任务操作增加可见 focus 样式与 `aria-label`。

### 任务执行面板

每个任务现在显示：

- 任务编号、难度和预计时间。
- 任务目标。
- “开始前”提示，包括投入时间和准备要求。
- “验收入口”命令模板，用于建立任务证据目录。
- 进行中、完成、稍后处理三种状态。
- 四步执行清单。
- 完成验收标准。
- 建议产出物。
- 常见偏差。
- 关联 GitHub 仓库和外部学习资源。

### 本地状态

- 保留原有 `radar-status-v3`、`radar-checks-v3` 和 `radar-favorites-v3` 数据。
- 新增 `radar-workbench-v1` 聚合快照，包含版本号和更新时间。
- 读取时优先兼容现有旧 key，未找到时从聚合快照恢复。
- 页面状态在任务面板和资源收藏变更后同步到聚合快照。
- 项目仍然是纯静态网站，不提供登录和跨设备云同步。

### 资源探索 URL 状态

- 资源探索页从 URL 读取 `q`、`category` 和 `resource` 参数。
- 搜索词和分类筛选变更时写回当前 URL。
- 可以通过链接恢复某个搜索结果、分类筛选或资源详情。

### 工作台导出

- 支持 JSON 导出，内容包括版本、导出时间、任务状态和收藏资源。
- 支持 Markdown 学习报告，内容包括总进度、已完成任务、进行中任务、收藏数量和下一步建议。
- 报告文件名分别为 `ai-engineering-training-ground.json` 和 `ai-engineering-training-ground.md`。

## 数据与内容边界

v3.1 不新增虚构数据，不把 GitHub Stars、课程链接或任务完成状态解释为就业市场统计。GitHub 指标继续以公开快照为依据；学习任务的完成标准仍然是用户自己的代码、命令、日志、评测、截图或可讲解的设计证据。

## 验证计划与结果

本版本执行了以下验证：

- `pnpm check`：通过。
- `pnpm build`：通过。
- 桌面端首页：验证内容不再被压缩为竖向窄列，原则区标题和正文恢复正常横向排版。
- 桌面端路线详情：验证任务 URL 参数可以直达指定任务，任务面板显示开始前信息、验收入口、步骤、验收标准和关联资源。
- 新增工具训练模块：验证 6 个工具任务和独立入口可访问。
- 面试准备模块：验证 6 个面试训练卡和关联复盘入口可访问。
- 移动端：使用 390×844 视口生成回归截图，确认页面可加载、单列规则和导航结构存在。

## 已知限制

1. 当前 `radar-workbench-v1` 是聚合快照，但任务状态、步骤和收藏仍保留旧 key 写入，以兼容既有版本；后续可以再统一为单一写入源。
2. Markdown 导出已实现，JSON 导入尚未加入；目前跨设备迁移仍需要手动处理备份文件。
3. 任务中的命令模板是证据目录起点，不是每个工具的完整安装教程；下一版本应补充 Python/TypeScript 示例和失败排查。
4. 资源探索当前已支持关键词和分类 URL 状态，主语言、岗位能力、难度、时间和排序仍可在下一版本继续增强。
5. 纯静态部署不支持账号、云同步、协作和多设备实时同步。

## v3.2 建议方向

- 完成统一状态对象和 JSON 导入。
- 增加任务前置条件、依赖关系、阶段门槛和下一任务推荐。
- 为核心任务补充可复制的 Python/TypeScript 命令、配置和输出模板。
- 工作台增加“今天继续、本周计划、我的证据”三个行动区域。
- 资源探索增加主语言、岗位能力、难度、投入时间和排序。
- 增加抽屉焦点管理、Escape 关闭和更完整的键盘操作验证。
